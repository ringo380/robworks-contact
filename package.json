{
  "name": "robworks-contact",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that receives contact-form POSTs from code.robworks.info and forwards them via Resend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.85.0"
  }
}
